# .github/workflows/lint.yaml
name: Lint, Type Check, Security, and Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'

      - name: Install Dependencies
        run: |
          uv venv
          uv pip install -e ".[dev]"

      # ──────────── Ruff (matches pre-commit) ────────────
      - name: Ruff Lint
        run: uv run ruff check .

      - name: Ruff Format Check
        run: uv run ruff format --check .

      # ──────────── Mypy (matches pre-commit) ────────────
      - name: Mypy Type Check
        run: uv run mypy .

      # ──────────── Bandit (matches pre-commit, reads pyproject.toml) ────────────
      - name: Bandit Security Scan
        run: uv run bandit -r src/ main.py -c pyproject.toml

      # ──────────── Tests (CI only) ────────────
      - name: Pytest
        run: uv run pytest

      # ──────────── Gitleaks (matches pre-commit) ────────────
      # - name: Gitleaks Secret Scan
      #   uses: gitleaks/gitleaks-action@v2
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
